# UlixEngine
Ulix is a Vulkan-backed Win32 GUI library. It uses logical coordinates for DPI-independent layout and hides all GPU complexity behind high-level primitives. Requires C++17 or later. The library is under active development.
